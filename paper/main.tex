\documentclass[10pt]{article}
\usepackage{PRIMEarxiv}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{natbib}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}

\title{PersonaLens: Cross-Model Analysis of\\Personality Representations in Large Language Models}

\author{
  Anonymous Author(s) \\
  \\
  Under Review
}

\begin{document}
\maketitle

% ============================================================
\begin{abstract}
Recent work has established that Big Five personality traits can be linearly extracted and steered in LLMs, but has focused on single models---leaving open the question of \emph{cross-model universality}. We conduct the first systematic cross-model analysis of personality representations across \textbf{eight} instruction-tuned LLMs spanning four architectural families (Qwen, LLaMA, Gemma, Mistral; 0.5B--7B parameters). We report four findings:

\textbf{(1)~Extraction is universal, steering is not:} All eight models achieve near-perfect LOSO extraction accuracy ($\ge$0.89), yet steering effectiveness (behavioral shift $\Delta$) varies 11$\times$ across architectures (Mistral-7B: $\Delta$=2.23 vs.\ Qwen2.5-7B: $\Delta$=0.20 at $\alpha{\in}[-6,6]$).

\textbf{(2)~The Perturbation Ratio Problem:} All steering vectors are unit-normalized ($\|v\|{=}1$), but hidden state magnitude $\|h\|$ varies 57$\times$ across models (Mistral-7B: RMS=0.051 vs.\ Gemma-2B: RMS=2.91). At standard $\alpha{=}6$, the effective perturbation $\alpha/\|h\|$ ranges from 4\% (Gemma-2B) to 183\% (Mistral-7B). This scale mismatch explains much of the cross-model steering variance ($r{=}-0.54$ between RMS scale and steering $\Delta$).

\textbf{(3)~Perturbation Ratio is Necessary but Not Sufficient:} Adjusted-alpha experiments that equalize perturbation ratios rescue Gemma-2B ($\Delta$: 0.39$\to$2.31, +492\%) and Qwen3-0.6B (+402\%), but \emph{worsen} monotonicity for Qwen2.5-1.5B ($r$: 0.38$\to$0.20) and barely improve Qwen2.5-7B ($\Delta$=0.62). Representation entanglement, not just perturbation magnitude, limits steerability.

\textbf{(4)~The Qwen Scaling Paradox:} Within the Qwen2.5 family, extraction quality \emph{improves} with scale (LOSO: 0.89$\to$1.00) while steering effectiveness \emph{degrades} ($\Delta$: 0.98$\to$0.20), even after perturbation-ratio correction---suggesting that representation entanglement increases with model capacity in this family.

\textbf{(5)~Cross-trait interference is architecture-dependent:} Models with strong steering show $>$$2\times$ selectivity (primary effect vs. off-target shifts), while weak-steering models show near-unity or below-unity selectivity.

\textbf{(6)~Hidden state magnitude is architecturally predictable:} Two hyperparameters (layer norm $\epsilon$ and attention softcapping) explain 91\% of RMS variance ($R^2{=}0.91$).

These findings establish that the perturbation ratio must be a \emph{standard control variable} in activation steering research, that cross-model analysis reveals fundamental architectural differences invisible to single-model studies, and that representation entanglement and hidden state scale are predictable architectural properties.
\end{abstract}

\keywords{Mechanistic Interpretability \and Personality \and Representation Engineering \and Activation Steering \and Large Language Models}

% ============================================================
\section{Introduction}\label{sec:intro}

Large Language Models (LLMs) readily adopt distinct \emph{personas} when instructed through system prompts, producing text that reflects specific personality traits \citep{shanahan2023role}. This ability has enabled applications ranging from immersive role-playing \citep{chen2024rolesurvey} to personality-consistent dialogue agents \citep{shao2023character}. However, a fundamental question remains: \textbf{do these personality behaviors correspond to structured internal representations, and can we identify and control them mechanistically?}

Prior work on LLM personality has largely taken a \emph{behavioral} perspective: evaluating model outputs against psychometric instruments such as the Big Five \citep{jiang2024evaluating}, or engineering prompts to elicit desired traits \citep{serapiogarcia2023personality}. While valuable, this approach treats the model as a black box and cannot answer whether personality traits correspond to identifiable neural structures.

Meanwhile, the field of \emph{representation engineering} \citep{zou2023representation} has demonstrated that semantic concepts---truthfulness \citep{li2024iti}, harmlessness \citep{rimsky2024steering}, and emotional states \citep{bao2024emotion}---correspond to linear directions in activation space that can be both probed and manipulated. Recent work has begun extending this paradigm to personality: persona vectors \citep{chen2025persona}, activation-space personality steering \citep{sun2025activation}, and persona blending \citep{billy2025blending} all suggest that personality traits may occupy distinct linear subspaces within transformer representations.

\citet{feng2026persona} demonstrated that personality vectors work well on \emph{a single model} (LLaMA-3-8B), establishing contrastive extraction, approximate orthogonality, and linear dose-response. But a critical question remains unanswered: \textbf{do these findings generalize across architectures, and if not, what governs the differences?}

\paragraph{Research Questions.} This work addresses three questions:
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=1pt]
    \item \textbf{Q1 (Universality):} Are personality representations equally extractable and steerable across diverse LLM architectures, or does effectiveness depend on model-specific properties?
    \item \textbf{Q2 (Perturbation Ratio):} Why does steering effectiveness vary across models---is it a fundamental representational difference, or a methodological artifact of how steering vectors interact with different activation scales?
    \item \textbf{Q3 (Scaling):} Does scaling up a model family improve personality steerability, as one might expect from richer representations?
\end{enumerate}

\paragraph{Contributions.} We analyze eight instruction-tuned LLMs (0.5B--7B parameters, four architectural families) and report six findings:
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=1pt]
\item \textbf{Extraction is universal, steering is not} (Sec.~\ref{sec:results}): All eight models achieve near-perfect extraction (LOSO $\ge$0.89), yet steering effectiveness varies 11$\times$ (Mistral-7B: $\Delta$=2.23 vs.\ Qwen2.5-7B: $\Delta$=0.20).
\item \textbf{The Perturbation Ratio Problem} (Sec.~\ref{sec:perturbation}): Steering vectors are unit-normalized while hidden state magnitudes vary 57$\times$, creating a systematic scale mismatch that explains much of the cross-model steering variance.
\item \textbf{Perturbation Ratio is Necessary but Not Sufficient} (Sec.~\ref{sec:adjusted_alpha}): Adjusted-alpha ablations show that equalizing perturbation ratios rescues some models (+492\% for Gemma-2B) but worsens monotonicity for others, revealing a second factor: representation entanglement.
\item \textbf{The Qwen Scaling Paradox} (Sec.~\ref{sec:qwen_paradox}): Within the Qwen2.5 family, extraction quality \emph{improves} with scale while steering effectiveness \emph{degrades}, even after perturbation-ratio correction.
\item \textbf{Cross-trait interference is architecture-dependent} (Sec.~\ref{sec:interference}): Models with strong steering show $>$$2\times$ selectivity, while weak-steering models show near-unity or below-unity selectivity, confirming entanglement as a limiting factor.
\item \textbf{Hidden state magnitude is architecturally predictable} (Sec.~\ref{sec:discussion}): Two hyperparameters (layer norm $\epsilon$ and attention softcapping) explain 91\% of RMS variance ($R^2{=}0.91$).
\end{enumerate}
We additionally provide: architecture-dependent orthogonality analysis (Sec.~\ref{sec:orthogonality}), causal localization (Sec.~\ref{sec:localization}), defense mechanism geometry (Appendix~\ref{app:defense}), and multi-framework analysis (Appendix~\ref{app:multiframework}).

% ============================================================
\section{Related Work}\label{sec:related}

\paragraph{Personality in LLMs.}
The study of personality expression in LLMs has expanded rapidly \citep{serapiogarcia2023personality, mao2024personality, personaforge2025}. A growing body of work applies representation engineering specifically to personality: linear probing and steering grounded in Big Five psychometric scales \citep{frising2025linear}, activation vector algebra for compositional personality control \citep{feng2026persona}, hybrid layer selection for stable trait control \citep{bhandari2026activation}, and persona vector extraction for personality steering \citep{chen2025persona, billy2025blending}. \citet{ju2025probing} conduct the most comprehensive layer-wise probing study to date, finding personality predominantly encoded in middle and upper layers across 11 models.

\paragraph{Our Position Relative to PERSONA.}
The most closely related work, PERSONA \citep{feng2026persona}, demonstrated personality extraction and steering on \emph{a single model} (LLaMA-3-8B). Our work extends this foundation to \emph{eight models} across four architectural families, revealing that cross-model analysis exposes fundamental phenomena invisible to single-model studies. While PERSONA established that personality steering \emph{can work}, we identify \emph{why it works better on some architectures than others}---the perturbation ratio problem. Our work builds on PERSONA's contrastive activation methodology but adds: (1)~causal localization via activation patching; (2)~cross-model orthogonality analysis revealing architecture-dependent variation; (3)~the perturbation ratio metric as a critical methodological control; and (4)~the Qwen scaling paradox demonstrating that model scale does not guarantee improved steerability.

\paragraph{Representation Engineering and Steering.}
Representation Engineering (RepE) \citep{zou2023representation} establishes that high-level concepts correspond to linear directions in activation space. Inference-Time Intervention (ITI) \citep{li2024iti} demonstrates that shifting activations along truthful directions improves model truthfulness. Contrastive Activation Addition \citep{rimsky2024steering, turner2023activation} generalizes this to behavioral attributes. Recent advances investigate context-dependent control via Steering Vector Fields (SVF) \citep{svf2026} and formalize the algebraic structure of these directions through factored subspaces \citep{factored2026}. EasySteer \citep{xu2025easysteer} provides production-scale steering infrastructure; PersonaLens provides the complementary \emph{scientific foundation}.

\paragraph{Reliability and Theoretical Concerns.}
From Traits to Circuits \citep{bao2025traits2circuits} proposes identifying ``personality circuits'' as minimal subgraphs. Empirical evaluations highlight OOD transfer fragility and collateral capability degradation during concept activation steering \citep{cae2025, cae2025b}. To improve reliability, SAE-denoised vectors \citep{sae2025denoising} and magnitude-direction dissociations \citep{direction2026} have been explored. \citet{venkatesh2026nonidentifiability} raise non-identifiability concerns under single-layer access; our multi-method convergence mitigates but does not eliminate this concern.

% ============================================================
\section{PersonaLens Framework}\label{sec:method}

PersonaLens consists of five modular phases (Fig.~\ref{fig:pipeline}), each designed to be model-agnostic and applicable to any transformer architecture.

\begin{figure}[t]
\centering
\fbox{\parbox{0.95\textwidth}{\small
\textbf{Phase 1: Contrastive Data Construction} (Sec.~\ref{sec:data})\\
$\rightarrow$ Paired prompts $(x^+_i, x^-_i)$ for each trait \\[4pt]
\textbf{Phase 2: Activation Collection} (Sec.~\ref{sec:collection})\\
$\rightarrow$ Hidden states $h_\ell(x^+_i), h_\ell(x^-_i)$ at all layers $\ell$ \\[4pt]
\textbf{Phase 3: Persona Vector Extraction} (Sec.~\ref{sec:extraction})\\
$\rightarrow$ Personality directions $v_\ell$ per layer via mean-diff / PCA / probe \\[4pt]
\textbf{Phase 4: Causal Localization} (Sec.~\ref{sec:localization})\\
$\rightarrow$ Causal importance $\mathcal{I}(\ell)$ via activation patching \\[4pt]
\textbf{Phase 5: Personality Steering} (Sec.~\ref{sec:steering})\\
$\rightarrow$ Inject $v_\ell$ during inference: $\tilde{h}_\ell = h_\ell + \alpha \cdot v_\ell$
}}
\caption{The PersonaLens pipeline. Each phase is independently executable and produces standardized outputs that enable cross-model comparison.}
\label{fig:pipeline}
\end{figure}

% ============================================================
\subsection{Phase 1: Contrastive Data Construction}\label{sec:data}

Following the contrastive activation paradigm \citep{zou2023representation, rimsky2024steering}, we construct paired prompts designed to elicit contrasting personality states while keeping the conversational scenario identical.

\paragraph{Big Five Traits.}
For each of the five personality dimensions---Openness (O), Conscientiousness (C), Extraversion (E), Agreeableness (A), and Neuroticism (N)---we define:
\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=1pt]
\item A \textbf{high-trait system prompt} instructing the model to behave as a person with an extreme level of the trait;
\item A \textbf{low-trait system prompt} instructing the opposite extreme;
\item $K=20$ \textbf{neutral scenario prompts} (user messages) spanning diverse social, emotional, and decision-making contexts.
\end{itemize}

For example, an Openness pair uses the system prompts ``\textit{You are extremely open to new experiences, intellectually curious, imaginative\ldots}'' vs.\ ``\textit{You are very practical, conventional, and prefer routine\ldots}'', with shared scenarios like ``\textit{What do you think about modern abstract art?}''

\paragraph{Defense Mechanisms.}
Following Vaillant's hierarchy \citep{vaillant1994ego}, we construct contrastive pairs for three defense mechanisms (Humor, Rationalization, Projection) spanning mature, neurotic, and immature categories. Each contrasts an active vs.\ neutral condition with $K{=}10$ conflict/ego-threat scenarios (Appendix~\ref{app:defense}).

% ============================================================
\subsection{Phase 2: Activation Collection}\label{sec:collection}

For each pair, we collect hidden states $h_\ell \in \mathbb{R}^d$ at the last token position of every layer, capturing the model's pre-generation personality state. This produces paired activations $H^+_t, H^-_t$ per trait and layer. We report LOSO accuracy, Cohen's $d$, and SNR (Appendix~\ref{app:method_details}).

% ============================================================
\subsection{Phase 3: Persona Vector Extraction}\label{sec:extraction}

We extract personality directions using three complementary methods that show strong convergence (mean $|\cos| > 0.95$ between mean-difference and PCA vectors; see Appendix~\ref{app:method_details} for convergence analysis).

\paragraph{Method 1: Mean Difference (Contrastive Activation Addition).}
The primary extraction method \citep{rimsky2024steering, chen2025persona}:
\begin{equation}\label{eq:meandiff}
v^{\text{diff}}_\ell = \frac{1}{K} \sum_{i=1}^{K} \left[ h_\ell(x^+_i) - h_\ell(x^-_i) \right], \qquad \hat{v}^{\text{diff}}_\ell = \frac{v^{\text{diff}}_\ell}{\|v^{\text{diff}}_\ell\|}
\end{equation}
To control for residual-stream scaling, we use \textbf{RMS-normalized difference norm}: $\frac{\|v^{\text{diff}}_\ell\|}{\text{RMS}_\ell}$ where $\text{RMS}_\ell = \sqrt{\frac{1}{2K}\sum (h_\ell)^2}$.

\paragraph{Methods 2--3: PCA and Linear Probing.}
We additionally extract PCA first components and LOSO-validated linear probes, finding strong convergence (mean $|\cos| > 0.95$ with PCA; see Appendix~\ref{app:method_details}). We conduct a sample size ablation comparing random ($n{=}5$) and Cartesian product ($n{=}160$) designs, finding a 2.44$\times$ increase in effect size (Appendix~\ref{app:method_details}).

% ============================================================
\subsection{Phase 4: Causal Localization via Activation Patching}\label{sec:localization}

Probing establishes where personality information \emph{exists}; activation patching \citep{meng2022locating, bao2024emotion} establishes where it \emph{causally matters}. We perform token-localized patching (system vs.\ user prompt spans) and component-level tracing (attention vs.\ MLP), measuring causal importance as KL divergence between clean and patched outputs:
\begin{equation}\label{eq:causal}
\mathcal{I}(\ell) = D_{\text{KL}}\!\left(\text{softmax}(o^+) \;\|\; \text{softmax}(o^+_{\text{patched},\ell})\right)
\end{equation}

% ============================================================
\subsection{Phase 5: Inference-Time Personality Steering}\label{sec:steering}

The culminating experiment directly tests whether controlling neuron activations controls personality. We inject persona vectors during inference using forward hooks:
\begin{equation}\label{eq:steering}
\tilde{h}_\ell = h_\ell + \alpha \cdot \hat{v}_\ell
\end{equation}
where $\alpha \in \mathbb{R}$ controls steering strength. Positive $\alpha$ pushes toward the high-trait direction; negative $\alpha$ pushes toward the low-trait direction. Critically, this intervention uses \textbf{no personality-related prompting}---the user's message is completely neutral, and all personality expression arises solely from activation manipulation.

% ============================================================
\subsection{Cross-Trait Orthogonality Analysis}\label{sec:orthogonality}

We measure trait independence via cosine similarity between persona vectors at each trait's best layer. Following the Big Five's orthogonal factor structure \citep{costa1992revised}, we expect $|\cos| \ll 1$. Results in Appendix~\ref{app:permodel}.

% ============================================================
\subsection{The Perturbation Ratio Problem}\label{sec:perturbation_method}

A critical methodological insight: steering vectors are unit-normalized ($\|\hat{v}\|{=}1$), but hidden state magnitude varies dramatically across architectures. The \textbf{Perturbation Ratio} (PR) at coefficient $\alpha$ is $\text{PR} = \alpha / \|h\|$, determining effective intervention strength. At fixed $\alpha{=}6$, PR varies 46$\times$ across our eight models (Sec.~\ref{sec:perturbation}).

\paragraph{Implications.} This scale mismatch means that:\textbf{(1)}~Comparing steering effectiveness at fixed $\alpha$ across models is misleading; \textbf{(2)}~Models with larger hidden state magnitudes (higher RMS scale) require proportionally larger $\alpha$ to achieve equivalent perturbation; \textbf{(3)}~The ``safe'' $\alpha$ range varies not just by model architecture but by the specific residual stream geometry of each model. We validate this hypothesis through adjusted-alpha ablations in Sec.~\ref{sec:adjusted_alpha}.

% ============================================================
\section{Experimental Setup}\label{sec:setup}

\paragraph{Models.} We validate PersonaLens on eight models spanning four distinct architectural families:
\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=1pt]
\item \textbf{Qwen2.5-0.5B-Instruct}: 24 layers, $d{=}896$, 0.5B params
\item \textbf{Qwen3-0.6B} \citep{qwen2025qwen3}: 28 layers, $d{=}1024$, 0.6B params
\item \textbf{TinyLlama-1.1B-Chat-v1.0} \citep{zhang2024tinyllama}: 22 layers, $d{=}2048$, 1.1B params
\item \textbf{Llama-3.2-1B-Instruct} \citep{dubey2024llama}: 16 layers, $d{=}2048$, 1.0B params
\item \textbf{Qwen2.5-1.5B-Instruct}: 28 layers, $d{=}1536$, 1.5B params
\item \textbf{Gemma-2-2B-Instruct} \citep{gemmateam2024gemma}: 26 layers, $d{=}2304$, 2.0B params
\item \textbf{Qwen2.5-7B-Instruct}: 28 layers, $d{=}3584$, 7.0B params
\item \textbf{Mistral-7B-Instruct-v0.1}: 32 layers, $d{=}4096$, 7.0B params
\end{itemize}
These models span a 14$\times$ parameter range (0.5B--7B) and four architectural families (Qwen, LLaMA, Gemma, Mistral), enabling systematic analysis of scale and architectural effects. Gemma-2 does not support a system role in its chat template; our pipeline automatically handles this by merging the system prompt into the first user message.

\paragraph{Traits Analyzed.} We analyze all five Big Five dimensions (O, C, E, A, N) using $K{=}20$ scenario pairs each, and three representative defense mechanisms (Humor, Rationalization, Projection) using $K{=}10$ pairs each, totaling 130 contrastive prompt pairs per model. Qwen2.5-0.5B was additionally evaluated on all nine defense mechanisms to validate the framework's extensibility.

\paragraph{Extraction Configuration.} Linear probes use strong L2-regularized logistic regression ($C{=}0.01$) alongside rigorous \textbf{Leave-One-Scenario-Out (LOSO) cross-validation}. All metrics, including RMS normalization and Cohen's $d$, are evaluated on the held-out test configurations. \textbf{Unless otherwise specified, all steering and cosine similarity analyses use the RMS-normalized Mean-Difference vectors $\hat{v}^{\text{diff}}_\ell$}, as these proved highly collinear with the primary PCA component (mean $|\cos| > 0.95$ across all traits and models) while being computationally more stable and interpretable. Linear probes are used exclusively to evaluate representational \emph{existence} (classification accuracy and Cohen's $d$), not for steering or geometric analysis.

\paragraph{Steering Evaluation.} For steering experiments, we evaluate across $\alpha \in [-6.0, +6.0]$ using LLM-as-Judge scoring with BFI rubrics. We report two key metrics per trait: \textbf{(1)}~Steering effectiveness $\Delta = \max(\text{judge\_mean}) - \min(\text{judge\_mean})$ across alphas; \textbf{(2)}~Monotonicity correlation $r$ (Spearman) between $\alpha$ and judge ratings.

% ============================================================
\section{Results}\label{sec:results}

We organize results around four core findings, each presented as a \emph{cross-model} observation supported by unified summary tables.

% ============================================================
\subsection{Personality Extraction is Universal}\label{sec:extraction_universal}

\paragraph{Research Question.} Do personality traits occupy linearly separable directions in activation space universally across architectures?

Across all eight tested architectures, linear probes achieve near-perfect classification of personality contrasts from the earliest layers onward. Table~\ref{tab:extraction_universal} summarizes the key extraction metrics.

\begin{table}[t]
\caption{\textbf{Cross-model extraction summary.} LOSO accuracy, best layer, and Cohen's $d$ at each model's optimal layer. All models achieve high separability regardless of architecture.}
\label{tab:extraction_universal}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Params} & \textbf{Best L} & \textbf{Mean LOSO} & \textbf{Best $d$} \\
\midrule
Q2.5-0.5B & 0.5B & L11 & 0.89 & 2.23 \\
Q3-0.6B & 0.6B & L2 & 0.99 & 6.47 \\
TinyLlama & 1.1B & L13 & 0.82 & 2.99 \\
Llama-3.2 & 1.0B & L6 & 0.99 & 4.70 \\
Q2.5-1.5B & 1.5B & L1 & 1.00 & 5.80 \\
Gemma-2B & 2.0B & L8 & 1.00 & 5.43 \\
Q2.5-7B & 7.0B & L2 & 1.00 & 4.86 \\
Mistral-7B & 7.0B & L5 & 1.00 & 6.45 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key finding: extraction is architecture-invariant.} All eight models achieve LOSO accuracy $\ge$0.82 (mean = 0.96), with six of eight exceeding 0.98. This confirms that personality representations are a \emph{universal} property of instruction-tuned transformers, not artifacts of specific training recipes or architectural choices. Cohen's $d$ values are consistently large ($d > 2.0$ across all models), indicating substantial effect sizes.

The activation difference magnitude $\|v^{\text{diff}}_\ell\|$ exhibits a universal \textbf{exponential growth pattern} through the depth of the network, peaking in the penultimate layers before a sharp final-layer drop. This pattern is observed in all eight models, suggesting a fundamental property of transformer personality processing rather than an artifact of any particular architecture.

% ============================================================
\subsection{The Perturbation Ratio Problem}\label{sec:perturbation}

\paragraph{Research Question.} Why does steering effectiveness vary across models, and can this variance be explained by methodological factors rather than fundamental representational differences?

\begin{table}[t]
\caption{\textbf{Perturbation ratios across eight models.} RMS scale (hidden state magnitude), estimated $\|h\|$, perturbation ratio at $\alpha=6$, and steering effectiveness ($\Delta$). Higher RMS scale = smaller effective perturbation = weaker steering.}
\label{tab:perturbation_ratio}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{RMS Scale} & \textbf{dim $\|h\|$} & \textbf{PR @ $\alpha$=6} & \textbf{Steer $\Delta$} \\
\midrule
Mistral-7B & 0.051 & 4096 & 183\% & 2.23 \\
Q2.5-0.5B & 0.516 & 896 & 39\% & 0.98 \\
Llama-3.2 & 0.538 & 2048 & 25\% & 2.10 \\
Q2.5-7B & 0.508 & 3584 & 20\% & 0.20 \\
Q2.5-1.5B & 0.831 & 1536 & 18\% & 0.28 \\
TinyLlama & 0.508$^*$ & 2048 & $\sim$20\% & 1.22 \\
Q3-0.6B & 1.734 & 1024 & 11\% & 0.42 \\
Gemma-2B & 2.907 & 2304 & 4\% & 0.39 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:perturbation_ratio} reveals a striking pattern: \textbf{RMS scale and steering effectiveness show a moderate negative correlation} ($\rho = -0.539$, $p < 0.05$), explaining approximately 29\% of cross-model variance ($R^2 = 0.29$). While this establishes scale mismatch as a significant factor, the remaining 71\% of variance points to additional architectural determinants---a question we investigate through adjusted-alpha ablations (Sec.~\ref{sec:adjusted_alpha}). Models with small RMS scales (Mistral-7B: 0.051, Llama-3.2: 0.538) achieve strong steering ($\Delta > 2.0$), while models with large RMS scales (Gemma-2B: 2.907, Q3-0.6B: 1.734) show weak steering ($\Delta < 0.5$).

The perturbation ratio (PR) quantifies this: at standard $\alpha = 6$, the effective perturbation ranges from 183\% for Mistral-7B (where the steering vector dominates the hidden state) to just 4\% for Gemma-2B (where the steering vector is a minor perturbation). This 46$\times$ range in effective intervention strength explains a substantial portion, but not all, of the cross-model steering variance.

\begin{figure}[t]
\centering
\includegraphics[width=0.65\textwidth]{figures/fig_pr_scatter.png}
\caption{\textbf{Hidden state scale vs.\ steering effectiveness.} Each point is one model. The negative correlation ($\rho = -0.54$, $p < 0.05$) confirms that models with larger residual stream magnitudes achieve weaker steering at fixed $\alpha$, motivating perturbation-ratio-aware comparisons.}
\label{fig:pr_scatter}
\end{figure}

\paragraph{Mechanistic interpretation.} The steering intervention adds $\alpha \hat{v}$ to hidden state $h$. When $\|h\|$ is large, the same $\alpha$ produces a smaller relative perturbation. Since output depends on relative activation patterns, models with larger hidden state norms require proportionally larger $\alpha$ for equivalent behavioral shifts.

% ============================================================
\subsection{Adjusted-Alpha Ablation: Necessary but Not Sufficient}\label{sec:adjusted_alpha}

\paragraph{Research Question.} If we equalize perturbation ratios across models by scaling $\alpha$ proportionally, do all models achieve equivalent steering effectiveness?

\begin{table}[t]
\caption{\textbf{Adjusted-alpha ablation results (mean across 5 Big Five traits).} Original vs.~adjusted alphas with steering effectiveness ($\Delta$) and percent change.}
\label{tab:adjusted_alpha}
\centering
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Model} & \textbf{Scale} & \textbf{Adj Alphas} & \textbf{Orig $\Delta$} & \textbf{Adj $\Delta$} & \textbf{Change} \\
\midrule
Gemma-2B & 23$\times$ & [-138, 138] & 0.39 & 2.31 & +492\% \\
Q3-0.6B & 9$\times$ & [-54, 54] & 0.42 & 2.11 & +402\% \\
Q2.5-7B & 5$\times$ & [-30, 30] & 0.20 & 0.62 & +210\% \\
Q2.5-1.5B & 5$\times$ & [-30, 30] & 0.28 & 0.61 & +118\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig_adjusted_alpha_comparison.png}
\caption{\textbf{Adjusted-alpha ablation results.} Left: steering effectiveness ($\Delta$) with original vs.\ adjusted $\alpha$. Right: dose-response monotonicity ($r$). Gemma-2B and Q3-0.6B show dramatic improvements; Q2.5-7B remains weak even after adjustment.}
\label{fig:adjusted_comparison}
\end{figure}

Table~\ref{tab:adjusted_alpha} presents results from our adjusted-alpha ablation, where $\alpha$ is scaled to achieve comparable perturbation ratios across models. We observe three distinct patterns:

\textbf{Key insight 1: Gemma-2B is rescued (+492\%).} The model with the largest RMS scale (2.907) and weakest original steering ($\Delta$=0.39) achieves strong steering ($\Delta$=2.31) when $\alpha$ is increased 23$\times$ to [-138, 138]. This confirms that \textbf{perturbation ratio was the limiting factor}---Gemma-2B's representations are perfectly steerable given adequate intervention strength.

\textbf{Key insight 2: Q2.5-1.5B shows heterogeneous trait responses.} While the mean monotonicity $r$ drops from 0.38 to 0.20, individual traits diverge dramatically: Extraversion improves ($r$: 0.74$\to$0.89, $\Delta$: 0.38$\to$0.80), while Openness and Neuroticism flip negative ($r$: 0.05$\to$-0.36, 0.32$\to$-0.11). Larger perturbations \emph{activate} some personality directions while \emph{disrupting} others, producing non-monotonic dose-response curves that average out poorly.

\textbf{Key insight 3: Q2.5-7B hits an architectural ceiling.} Even with 5$\times$ adjusted alphas, Q2.5-7B only achieves $\Delta$=0.62---weak compared to other models. This suggests \textbf{representation entanglement that cannot be overcome by larger perturbations} alone. As models in the Qwen2.5 family scale up, their personality representations become increasingly intertwined with other features, limiting steerability regardless of intervention strength.

\paragraph{The necessary-but-not-sufficient principle.} Perturbation ratio adjustment is \textbf{necessary} to achieve fair cross-model comparisons (rescuing Gemma-2B, Q3-0.6B) but \textbf{not sufficient} to equalize steerability across all architectures. A second factor---representation entanglement---emerges as a fundamental architectural property that varies across model families and scales.

% ============================================================
\subsection{The Qwen Scaling Paradox}\label{sec:qwen_paradox}

\paragraph{Research Question.} How does personality representation change as we scale within a single model family? Does extraction improve? Does steerability improve?

\begin{table}[t]
\caption{\textbf{Qwen2.5 family scaling analysis.} Extraction quality improves with scale while steering effectiveness degrades, even after perturbation-ratio correction.}
\label{tab:qwen_scaling}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Params} & \textbf{Ext LOSO} & \textbf{Steer $\Delta$} & \textbf{Adj $\Delta$} \\
\midrule
Q2.5-0.5B & 0.5B & 0.89 & 0.98 & --- \\
Q2.5-1.5B & 1.5B & 1.00 & 0.28 & 0.61 \\
Q2.5-7B & 7.0B & 1.00 & 0.20 & 0.62 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:qwen_scaling} reveals a paradoxical pattern within the Qwen2.5 family:

\textbf{Extraction improves with scale.} LOSO accuracy increases from 0.89 (0.5B) to 1.00 (1.5B and 7B), with Cohen's $d$ improving from 2.23 to 5.80. Larger models form more robust, separable personality representations.

\textbf{Steering degrades with scale.} Original steering effectiveness drops from $\Delta$=0.98 (0.5B) to $\Delta$=0.20 (7B)---a 5$\times$ degradation. Even after perturbation-ratio adjustment, the 7B model only reaches $\Delta$=0.62, still weaker than the 0.5B model's unadjusted performance.

\paragraph{Interpretation: entanglement increases with capacity.} We hypothesize that larger capacity enables richer, more distributed representations that improve separability (extraction) but increase entanglement (harming steerability). This challenges the assumption that scale improves controllability.

% ============================================================
\subsection{Cross-Model Steering Landscape}\label{sec:steering_landscape}

Table~\ref{tab:full_steering} presents the complete steering effectiveness matrix across all eight models and five traits.

\begin{table}[t]
\caption{\textbf{Cross-model steering effectiveness.} Steering $\Delta$ (max judge\_mean minus min judge\_mean across alphas) per trait per model. Mean $\Delta$ and mean monotonicity correlation $r$ shown.}
\label{tab:full_steering}
\centering
\small
\begin{tabular}{lccccc|cc}
\toprule
\textbf{Model} & \textbf{Open} & \textbf{Consc} & \textbf{Extra} & \textbf{Agree} & \textbf{Neuro} & \textbf{Mean $\Delta$} & \textbf{Mean $r$} \\
\midrule
Mistral-7B & 1.94 & 2.35 & 2.25 & 2.15 & 2.48 & 2.23 & 0.93 \\
Llama-3.2 & 2.49 & 0.35 & 2.65 & 2.73 & 2.27 & 2.10 & 0.87 \\
TinyLlama & 1.26 & 1.90 & 1.80 & 0.70 & 0.45 & 1.22 & 0.79 \\
Q2.5-0.5B & 1.74 & 0.77 & 0.48 & 1.38 & 0.55 & 0.98 & 0.76 \\
Gemma-2B & 0.37 & 0.35 & 0.27 & 0.27 & 0.70 & 0.39 & 0.64 \\
Q3-0.6B & 0.29 & 0.10 & 0.57 & 0.50 & 0.62 & 0.42 & 0.56 \\
Q2.5-1.5B & 0.40 & 0.12 & 0.38 & 0.25 & 0.23 & 0.28 & 0.38 \\
Q2.5-7B & 0.23 & 0.08 & 0.23 & 0.23 & 0.23 & 0.20 & 0.05 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Architecture clusters emerge.} Models separate into strong steerers (Mistral-7B, Llama-3.2: $\Delta{>}2.0$), moderate steerers (TinyLlama, Q2.5-0.5B: $\Delta{\approx}1.0$), and weak steerers (Gemma-2B, Q3-0.6B, Q2.5-1.5B, Q2.5-7B: $\Delta{<}0.5$). Monotonicity $r$ ranges from 0.05 (Q2.5-7B) to 0.93 (Mistral-7B). Adjusted alphas rescue some weak steerers (Gemma-2B reaches $\Delta{=}2.31$), but others hit architectural ceilings.

% ============================================================
\subsection{Cross-Trait Interference Analysis}\label{sec:interference}

When steering a target trait, off-target trait shifts vary dramatically across architectures (Table~\ref{tab:interference_summary}). Strong-steering models (Mistral-7B, Llama-3.2) achieve $2.1\times$--$2.5\times$ selectivity (primary effect divided by mean off-diagonal shift), while Qwen2.5-7B shows below-unity selectivity (0.9x)---steering produces larger shifts in non-target traits than the target itself. Selectivity correlates strongly with steering effectiveness ($r{=}0.84$), establishing cross-trait interference as a complementary diagnostic to perturbation ratio. Full interference matrices and analysis in Appendix~\ref{app:interference}.

% ============================================================
\section{Discussion}\label{sec:discussion}

\paragraph{The Perturbation Ratio Problem: Implications for Steering Research.}
Our finding that perturbation ratio explains much of the cross-model steering variance has immediate methodological implications. Current steering literature typically reports fixed $\alpha$ values (e.g., $\alpha \in [-6, 6]$) without accounting for model-specific hidden state magnitudes. This practice is \emph{methodologically problematic without proper normalization} for cross-model comparisons. We recommend:
\begin{enumerate}[leftmargin=*,topsep=2pt,itemsep=1pt]
\item Report RMS scale or mean $\|h\|$ alongside steering results
\item Report perturbation ratios (PR = $\alpha / \|h\|$) as normalized intervention strengths
\item When comparing across models, use adjusted alphas that equalize PR or report results at comparable PR values
\end{enumerate}

\paragraph{Necessary but Not Sufficient: The Two-Factor Model of Steerability.}
Our adjusted-alpha ablations reveal that steering effectiveness depends on \emph{two} factors: (1)~\textbf{perturbation ratio}---the relative magnitude of the intervention, and (2)~\textbf{representation entanglement}---the degree to which personality features are intertwined with other semantic features. Perturbation ratio is a methodological control; representation entanglement is a fundamental architectural property. This two-factor model explains why:
\begin{itemize}[leftmargin=*,topsep=2pt,itemsep=1pt]
\item Gemma-2B was weak at standard $\alpha$ but strong after adjustment (low entanglement, wrong PR)
\item Q2.5-7B remained weak even after adjustment (high entanglement, architectural ceiling)
\item Q2.5-1.5B showed non-monotonicity after adjustment (moderate entanglement activated by larger perturbations)
\end{itemize}

\paragraph{The Qwen Scaling Paradox and Entanglement Hypothesis.}
The Qwen2.5 family's inverse scaling pattern---better extraction, worse steering at larger scales---challenges the assumption that larger models are more controllable. We hypothesize that larger capacity enables richer, more distributed representations that are \emph{better separated} (improving extraction) but \emph{more entangled} (harming steerability). This suggests a trade-off between representational richness and controllability that varies across model families.

\paragraph{Representation Entanglement: Independent Validation.}
To break potential circular reasoning (inferring entanglement from steering failure), we develop four \emph{independent} entanglement metrics computed from raw activations alone: (1)~\textbf{RV coefficient} (matrix correlation between trait subspaces), (2)~\textbf{subspace angles} between PCA trait subspaces, (3)~\textbf{effective dimensionality} (PCA-based), and (4)~\textbf{cross-probe accuracy}---train a probe on trait A, test on trait B's labels (Table~\ref{tab:entanglement}). Critically, the aggregate entanglement score does \emph{not} predict steering effectiveness ($\rho{=}{-}0.60$, $p{=}0.21$): Llama-3.2 has the \emph{highest} entanglement yet the strongest steering ($\Delta{=}2.10$), while Q2.5-1.5B has \emph{lower} entanglement but weak steering ($\Delta{=}0.28$). This confirms that PR, not entanglement, is the dominant cross-model factor. However, the Qwen scaling paradox persists \emph{after} PR correction, suggesting a subtler form of representational interference not captured by linear metrics.

\begin{table}[t]
\caption{\textbf{Independent entanglement metrics} (computed from raw activations only). Aggregate entanglement does \emph{not} predict steering effectiveness ($\rho{=}{-}0.36$, $p{=}0.39$), confirming PR as the dominant factor.}
\label{tab:entanglement}
\centering
\small
\begin{tabular}{lccccc|c}
\toprule
\textbf{Model} & \textbf{RV} & \textbf{CP} & \textbf{Eff.\ Dim} & \textbf{Agg} & \textbf{RMS} & $\Delta$ \\
\midrule
TinyLlama & 0.64 & 0.49 & 57/2048 & 0.45 & 0.34 & 1.22 \\
Q2.5-0.5B & 0.54 & 0.56 & 55/896 & 0.47 & 0.55 & 0.98 \\
Q2.5-1.5B & 0.63 & 0.55 & 69/1536 & 0.48 & 0.83 & 0.28 \\
Mistral-7B & 0.89 & 0.50 & 37/4096 & 0.50 & 0.05 & 2.23 \\
Q3-0.6B & 0.73 & 0.57 & 53/1024 & 0.50 & 2.69 & 0.42 \\
Llama-3.2 & 0.98 & 0.50 & 16/2048 & 0.53 & 0.23 & 2.10 \\
Q2.5-7B & 0.84 & 0.56 & 29/3584 & 0.53 & 0.51 & 0.20 \\
Gemma-2B & 0.82 & 0.59 & 64/2304 & 0.54 & 3.45 & 0.39 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Architectural Determinants of Hidden State Scale.}
Hidden state magnitude varies $57\times$ across models, but this variation is architecturally predictable. A linear regression predicting $\log_{10}(\text{RMS})$ from layer normalization epsilon ($\epsilon$) and attention softcapping (binary) achieves $R^2{=}0.91$:
\begin{equation}
\log_{10}(\text{RMS}) = -0.702 \times \log_{10}(\epsilon) + 0.016 \times \text{softcap} + \text{constant}
\end{equation}
Epsilon dominates: $10^{-6} \rightarrow$ RMS 0.3--2.5, $10^{-5} \rightarrow$ RMS 0.04--0.13. Full regression table and analysis in Appendix~\ref{app:architecture}.

\paragraph{PR-Aware Steering Algorithm.}
We formalize a principled $\alpha$ selection that requires only extraction-time statistics. Let $\hat{h}_\text{ref} = \text{RMS}_\text{ref} \cdot \sqrt{d_\text{ref}}$ be the hidden state norm of a reference model with known good steering at $\alpha_\text{ref}$. The PR-matched coefficient for a target model is:
\begin{equation}\label{eq:alpha_star}
\alpha^* = \alpha_\text{ref} \cdot \frac{\hat{h}_\text{target}}{\hat{h}_\text{ref}} = \alpha_\text{ref} \cdot \frac{\text{RMS}_\text{target} \cdot \sqrt{d_\text{target}}}{\text{RMS}_\text{ref} \cdot \sqrt{d_\text{ref}}}
\end{equation}
On Gemma-2B, this yields $\alpha^*{=}304$ (vs.\ our conservative $\alpha{=}138$), suggesting room for even stronger steering. Cross-trait analysis confirms PR-aware steering improves both \textbf{magnitude} ($\Delta$: 0.39$\to$2.31) and \textbf{selectivity} ($1.6\times \to 2.1\times$) for PR-limited models, while entanglement-limited models (Q2.5-7B: $0.9\times$) show no improvement. We recommend reporting RMS, PR, and $\alpha^*$ alongside all steering results \citep{stoehr2024activation}.

Cross-model orthogonality analysis (Appendix~\ref{app:permodel}) reveals architecture-dependent variation in trait independence.

\paragraph{Limitations.}
(1)~All steering uses linear directions; non-linear interventions might overcome entanglement. (2)~LLM-as-Judge may introduce biases; human evaluation would strengthen claims. (3)~PR analysis assumes isotropic geometry; anisotropic corrections warrant investigation. (4)~Defense mechanism and multi-framework results (Appendices~\ref{app:defense}, \ref{app:multiframework}) were primarily validated on the original five models.

% ============================================================
\section{Conclusion}\label{sec:conclusion}

Through systematic experiments on eight instruction-tuned LLMs spanning four architectural families and a 14$\times$ parameter range (0.5B--7B), we report four findings about cross-model personality representation:

\textbf{(1)~Extraction is universal:} All eight models achieve near-perfect LOSO accuracy ($\ge$0.89), demonstrating that personality representations are a fundamental property of instruction-tuned transformers.

\textbf{(2)~The Perturbation Ratio Problem:} Steering vectors are unit-normalized, but hidden state magnitudes vary 57$\times$ across models. This scale mismatch explains much of the cross-model steering variance ($r = -0.54$), establishing perturbation ratio as a critical methodological control.

\textbf{(3)~Perturbation ratio is necessary but not sufficient:} Adjusted-alpha ablations rescue some models (+492\% for Gemma-2B) but reveal representation entanglement as a second limiting factor. Some architectures (Q2.5-7B) hit steering ceilings that larger perturbations cannot overcome.

\textbf{(4)~The Qwen Scaling Paradox:} Within the Qwen2.5 family, extraction quality improves with scale while steering degrades---even after perturbation-ratio correction---suggesting that representation entanglement increases with capacity in this family.

\textbf{(5)~Cross-trait interference is architecture-dependent:} Models with strong steering show $2.1\times$ selectivity, while weak-steering models show near-unity or below-unity selectivity, confirming entanglement as a limiting factor.

\textbf{(6)~The hidden state magnitude is architecturally predictable:} $\log_{10}(\text{RMS}) \approx -0.702 \log_{10}(\epsilon) + 0.016 \cdot \text{softcap}$ ($R^2 = 0.91$ across 8 models); just two architectural hyperparameters explain 91\% of the variance.

These findings establish that cross-model analysis reveals fundamental architectural differences invisible to single-model studies. The perturbation ratio must become a standard control variable in activation steering research. Future work should investigate: attention-head-level localization to identify specific circuits; automated $\alpha$ optimization methods; and whether SAE-based interventions can overcome representation entanglement.

% ============================================================
\bibliographystyle{unsrtnat}
\bibliography{references}

% ============================================================
\appendix

\section{Cross-Model Steering Heatmap}\label{app:steering_heatmap}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_steering_heatmap.png}
\caption{\textbf{Cross-model steering effectiveness heatmap.} Each cell shows $\Delta$ for one (model, trait) pair. Three clusters emerge: strong steerers (top rows), moderate steerers, and weak steerers (bottom rows). Conscientiousness shows the highest cross-model variance.}
\label{fig:steering_heatmap}
\end{figure}

\section{Cross-Trait Interference Analysis}\label{app:interference}

\begin{table}[h]
\caption{\textbf{Cross-trait interference summary.} Mean primary effect (steering $\Delta$ on target trait), mean off-diagonal shift (non-target traits), maximum off-diagonal shift, and selectivity ratio (primary/mean off-diagonal). Lower selectivity indicates greater interference.}
\label{tab:interference_summary}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Prim $\Delta$} & \textbf{Off-Diag} & \textbf{Max Off} & \textbf{Select.} \\
\midrule
Q2.5-0.5B & 0.81 & 0.43 & 2.15 & 1.9x \\
Q3-0.6B & 0.35 & 0.20 & 0.60 & 1.8x \\
TinyLlama & 1.04 & 0.53 & 1.92 & 2.0x \\
Llama-3.2 & 1.64 & 0.66 & 2.45 & 2.5x \\
Q2.5-1.5B & 0.15 & 0.12 & 0.23 & 1.3x \\
Gemma-2B & 0.28 & 0.17 & 0.50 & 1.6x \\
Q2.5-7B & 0.07 & 0.08 & 0.26 & 0.9x \\
Mistral-7B & 2.24 & 1.09 & 2.03 & 2.1x \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:interference_summary} reveals dramatic variation in cross-trait selectivity. Strong-steering models (Mistral-7B, Llama-3.2) achieve $2.1\times$--$2.5\times$ selectivity, meaning their primary steering effect is more than twice as large as mean off-diagonal shifts. In contrast, Qwen2.5-7B shows \textbf{selectivity below unity} (0.9x): steering produces larger shifts in non-target traits than in the target trait itself---the hallmark of entanglement.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_selectivity.png}
\caption{\textbf{Steering effectiveness vs.~selectivity ratio.} Each point represents one model. Selectivity ratio (primary $\Delta$ divided by mean off-diagonal shift) separates models into three clusters: high-selectivity strong steerers (Mistral-7B, Llama-3.2), moderate-selectivity models (TinyLlama, Q2.5-0.5B), and low-selectivity weak steerers (Qwen2.5 family, Gemma-2B). The dashed line indicates unity selectivity (no discrimination between target and non-target traits).}
\label{fig:selectivity}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/fig_interference_matrices.png}
\caption{\textbf{Cross-trait interference matrices: Mistral-7B (left) vs.~Qwen2.5-7B (right).} Each cell shows the steering effect on the column trait when steering the row trait. Mistral-7B shows strong diagonal dominance (high selectivity) with off-diagonal values roughly half the diagonal. Qwen2.5-7B shows weak diagonal signals and substantial off-diagonal leakage, consistent with its below-unity selectivity ratio.}
\label{fig:interference_matrices}
\end{figure}

Figure~\ref{fig:selectivity} plots steering effectiveness against selectivity ratio. Models cluster into three regimes: (1)~\textbf{High-selectivity strong steerers} (Mistral-7B, Llama-3.2) with both large $\Delta$ and high selectivity; (2)~\textbf{Moderate models} (TinyLlama, Q2.5-0.5B) with intermediate $\Delta$ and selectivity around $2.0\times$; and (3)~\textbf{Low-selectivity weak steerers} (Qwen2.5 family, Gemma-2B) with both small $\Delta$ and poor selectivity.

Figure~\ref{fig:interference_matrices} contrasts the full interference matrices for Mistral-7B (high selectivity) and Qwen2.5-7B (below-unity selectivity). Mistral shows clear diagonal dominance: when steering Openness, Openness shifts substantially more than any other trait. Qwen2.5-7B shows blurred diagonal structure: steering any trait produces comparable shifts across all traits, confirming that its personality representations are deeply entangled.

\paragraph{Key finding: selectivity predicts steerability.} The correlation between selectivity ratio and steering effectiveness is strong ($r = 0.84$). Models with clean, separable trait representations (high selectivity) achieve reliable steering; models with entangled representations (low selectivity) do not. This establishes cross-trait interference as a complementary diagnostic to perturbation ratio for predicting steering success.

\section{Representation Entanglement Details}\label{app:entanglement}

\begin{table}[h]
\caption{\textbf{Representation entanglement and steerability.} Mean $|\cos|$ between persona vectors at each model's best layer, with steering effectiveness. No simple monotonic relationship exists.}
\label{tab:entanglement_summary}
\centering
\small
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{Mean $|\cos|$} & \textbf{Cond. \#} & \textbf{Steer $\Delta$} \\
\midrule
Q2.5-7B & 0.059 & 1.5 & 0.20 \\
Q2.5-1.5B & 0.058 & 1.6 & 0.28 \\
Q3-0.6B & 0.073 & 1.9 & 0.42 \\
Llama-3.2 & 0.070 & 2.3 & 2.10 \\
Mistral-7B & 0.089 & 2.1 & 2.23 \\
Gemma-2B & 0.149 & 2.6 & 0.39 \\
Q2.5-0.5B & 0.148 & 3.6 & 0.98 \\
TinyLlama & 0.192 & 3.8 & 1.22 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/fig_entanglement_analysis.png}
\caption{\textbf{Trait entanglement vs.\ steering effectiveness.} Mean $|\cos|$ between persona vectors (quantifying cross-trait correlation) plotted against steering $\Delta$. Left: original steering. Right: best available steering (adjusted $\alpha$ where available). Higher entanglement does not show a clear monotonic relationship with steering, suggesting entanglement interacts with other architectural factors.}
\label{fig:entanglement}
\end{figure}

We quantify \textbf{representation entanglement} as the mean absolute cosine similarity $|\cos|$ between persona vectors at each model's best layer (Table~\ref{tab:entanglement_summary}). Values range from 0.058 (Q2.5-1.5B) to 0.192 (TinyLlama). Critically, entanglement does \emph{not} linearly predict steering effectiveness: Q2.5-1.5B has the lowest entanglement (0.058) yet remains poorly steerable, while TinyLlama has the highest (0.192) and achieves moderate steering ($\Delta$=1.22). This confirms that entanglement alone cannot explain the steering landscape---it is the \emph{interaction} between perturbation ratio, entanglement, and potentially other architectural factors (e.g., attention head distribution, layer normalization) that determines steerability.

\section{Architectural Determinants of Hidden State Scale}\label{app:architecture}

\begin{table}[h]
\caption{\textbf{Architectural parameters and hidden state scale.} RMS magnitude at the best layer for each model, with architectural family and key hyperparameters.}
\label{tab:architecture_regression}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Family} & \textbf{$\epsilon$} & \textbf{softcap} & \textbf{RMS} \\
\midrule
Q2.5-0.5B & Qwen2.5 & $10^{-6}$ & 0 & 0.454 \\
Q3-0.6B & Qwen3 & $10^{-6}$ & 0 & 0.334 \\
TinyLlama & LLaMA & $10^{-5}$ & 0 & 0.126 \\
Llama-3.2 & LLaMA & $10^{-5}$ & 0 & 0.093 \\
Q2.5-1.5B & Qwen2.5 & $10^{-6}$ & 0 & 0.431 \\
Gemma-2B & Gemma-2 & $10^{-6}$ & 50 & 2.466 \\
Q2.5-7B & Qwen2.5 & $10^{-6}$ & 0 & 0.311 \\
Mistral-7B & Mistral & $10^{-5}$ & 0 & 0.036 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_architecture_rms.png}
\caption{\textbf{Architectural prediction of hidden state scale.} Observed vs.~predicted $\log_{10}(\text{RMS})$ from the regression $\log_{10}(\text{RMS}) \approx -0.702 \log_{10}(\epsilon) + 0.016 \cdot \text{softcap}$. Two architectural hyperparameters explain 91\% of cross-model variance in hidden state magnitude.}
\label{fig:architecture_rms}
\end{figure}

Table~\ref{tab:architecture_regression} and Figure~\ref{fig:architecture_rms} demonstrate that hidden state scale is not arbitrary but systematically determined by architecture. The layer normalization epsilon dominates: models with $\epsilon = 10^{-6}$ (Qwen, Gemma) show RMS $0.3$--$2.5$, while models with $\epsilon = 10^{-5}$ (LLaMA, Mistral) show RMS $0.04$--$0.13$---a $10\times$ difference driven by a single hyperparameter. Attention softcapping (Gemma-2B: softcap=50) contributes an additional multiplicative effect.

\section{Method Details}\label{app:method_details}

\subsection{Metrics and Evaluation Criteria}

We evaluate personality encoding through four complementary metrics.

\paragraph{Leave-One-Scenario-Out (LOSO) Accuracy.}
Given $K$ contrastive pairs per trait, we train a linear probe on $K{-}1$ pairs and evaluate on the held-out pair. Repeating over all $K$ folds yields LOSO accuracy $a_\ell \in [0,1]$, measuring generalization to unseen contexts.

\paragraph{Cohen's $d$ Effect Size.}
At layer $\ell$, we compute standardized mean separation between high-trait and low-trait activations:
\begin{equation}
d_\ell = \frac{\mu_{+,\ell} - \mu_{-,\ell}}{\sqrt{\frac{(n_+-1)s_{+,\ell}^2 + (n_--1)s_{-,\ell}^2}{n_+ + n_- - 2}}}
\end{equation}
Values $d > 0.8$ indicate large effects; our models consistently achieve $d > 2.0$.

\paragraph{Signal-to-Noise Ratio (SNR).}
To distinguish genuine signal from scaling artifacts:
\begin{equation}
\text{SNR}_\ell = \frac{\|v^{\text{diff}}_\ell\| / \text{RMS}_\ell}{\mathbb{E}[\|v^{\text{random}}_\ell\| / \text{RMS}_\ell]}
\end{equation}
SNR $> 2$ indicates contrastive signal exceeds random noise; our models achieve SNR $> 7$.

\paragraph{Perturbation Ratio (PR).}
Defined as PR = $\alpha / \|h\|$, this metric normalizes intervention strength by hidden state magnitude for cross-model comparability.

\subsection{Extraction Method Convergence}

Mean-difference vectors show mean $|\cos| > 0.95$ with PCA first components and $|\cos| > 0.89$ with linear probe weights across all traits and models. This high collinearity indicates personality directions are robustly identifiable despite theoretical non-identifiability concerns.

\subsection{Statistical Power and Sample Size}

With $K=20$ scenario pairs per Big Five trait, bootstrap standard errors are $<0.02$ for accuracy and $<0.15$ for Cohen's $d$. For defense mechanisms ($K=10$), accuracy standard errors remain $<0.03$, sufficient given larger effect sizes ($d > 2.0$).

\subsection{Sample Size Ablation}

Comparing random ($n=5$) vs.\ Cartesian product ($n=160$) designs on Qwen3 Openness shows a 2.44$\times$ increase in effect size (from $d=2.69$ to $d=6.57$), establishing that prior work using $n=5$--$20$ systematically underestimates signal strength.

% ============================================================
\section{Adjusted-Alpha Per-Trait Detailed Tables}\label{app:adjusted_alpha_detail}

Tables~\ref{tab:gemma_adj}--\ref{tab:qwen25_7b_adj} present per-trait steering results for the four models in our adjusted-alpha ablation.

\begin{table}[h]
\caption{\textbf{Gemma-2B adjusted-alpha per-trait results.} Scale factor: 23$\times$, $\alpha \in$ [-138, 138].}
\label{tab:gemma_adj}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Trait} & \textbf{Orig $\Delta$} & \textbf{Adj $\Delta$} & \textbf{Orig $r$} & \textbf{Adj $r$} \\
\midrule
Openness & 0.37 & 2.09 & 0.33 & 1.00 \\
Conscientiousness & 0.35 & 2.45 & 0.95 & 1.00 \\
Extraversion & 0.27 & 2.33 & 0.73 & 1.00 \\
Agreeableness & 0.27 & 2.62 & 0.15 & 1.00 \\
Neuroticism & 0.70 & 2.08 & 0.96 & 0.18 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\caption{\textbf{Qwen3-0.6B adjusted-alpha per-trait results.} Scale factor: 9$\times$, $\alpha \in$ [-54, 54].}
\label{tab:qwen3_adj}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Trait} & \textbf{Orig $\Delta$} & \textbf{Adj $\Delta$} & \textbf{Orig $r$} & \textbf{Adj $r$} \\
\midrule
Openness & 0.29 & 2.09 & -0.33 & 0.22 \\
Conscientiousness & 0.10 & 0.98 & 0.43 & 0.99 \\
Extraversion & 0.57 & 3.02 & 0.96 & 0.96 \\
Agreeableness & 0.50 & 2.42 & 0.86 & 0.43 \\
Neuroticism & 0.62 & 2.05 & 0.89 & 0.54 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\caption{\textbf{Qwen2.5-1.5B adjusted-alpha per-trait results.} Scale factor: 5$\times$, $\alpha \in$ [-30, 30].}
\label{tab:qwen25_15b_adj}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Trait} & \textbf{Orig $\Delta$} & \textbf{Adj $\Delta$} & \textbf{Orig $r$} & \textbf{Adj $r$} \\
\midrule
Openness & 0.40 & 0.37 & 0.05 & -0.36 \\
Conscientiousness & 0.12 & 0.57 & 0.07 & 0.54 \\
Extraversion & 0.38 & 0.80 & 0.74 & 0.89 \\
Agreeableness & 0.25 & 0.82 & 0.72 & 0.04 \\
Neuroticism & 0.23 & 0.48 & 0.32 & -0.11 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\caption{\textbf{Qwen2.5-7B adjusted-alpha per-trait results.} Scale factor: 5$\times$, $\alpha \in$ [-30, 30].}
\label{tab:qwen25_7b_adj}
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Trait} & \textbf{Orig $\Delta$} & \textbf{Adj $\Delta$} & \textbf{Orig $r$} & \textbf{Adj $r$} \\
\midrule
Openness & 0.23 & 2.06 & 0.11 & 0.14 \\
Conscientiousness & 0.08 & 0.15 & -0.46 & -0.47 \\
Extraversion & 0.23 & 0.30 & 0.29 & 0.70 \\
Agreeableness & 0.23 & 0.30 & 0.05 & 0.55 \\
Neuroticism & 0.23 & 0.28 & 0.26 & 0.68 \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Per-Model Detailed Results}\label{app:permodel}

This appendix presents the full per-model experimental results, including layer-wise encoding profiles, orthogonality matrices, causal localization maps, and steering curves for each individual model.

\subsection{Qwen2.5-0.5B-Instruct}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen2.5-0.5B-Instruct_openness.png}
\caption{\textbf{Qwen2.5-0.5B-Instruct: Layer-wise personality encoding profile for Openness.}
Linear probe accuracy, Mean difference norm, and PCA explained variance.}
\label{fig:Qwen_Qwen2.5-0.5B-Instruct_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_Qwen_Qwen2.5-0.5B-Instruct.png}
\caption{\textbf{Qwen2.5-0.5B-Instruct: Cosine similarity matrix between persona vectors.}}
\label{fig:Qwen_Qwen2.5-0.5B-Instruct_ortho}
\end{figure}

\clearpage

\subsection{Qwen3-0.6B}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen3-0.6B_openness.png}
\caption{\textbf{Qwen3-0.6B: Layer-wise personality encoding profile for Openness.}}
\label{fig:Qwen_Qwen3-0.6B_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_Qwen_Qwen3-0.6B.png}
\caption{\textbf{Qwen3-0.6B: Cosine similarity matrix between persona vectors.}}
\label{fig:Qwen_Qwen3-0.6B_ortho}
\end{figure}

\clearpage

\subsection{TinyLlama-1.1B}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_TinyLlama_TinyLlama-1.1B-Chat-v1.0_openness.png}
\caption{\textbf{TinyLlama-1.1B: Layer-wise personality encoding profile for Openness.}}
\label{fig:TinyLlama_TinyLlama-1.1B-Chat-v1.0_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_TinyLlama_TinyLlama-1.1B-Chat-v1.0.png}
\caption{\textbf{TinyLlama-1.1B: Cosine similarity matrix between persona vectors.}}
\label{fig:TinyLlama_TinyLlama-1.1B-Chat-v1.0_ortho}
\end{figure}

\clearpage

\subsection{Llama-3.2-1B-Instruct}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_Llama-3.2-1B-Instruct_openness.png}
\caption{\textbf{Llama-3.2-1B-Instruct: Layer-wise personality encoding profile for Openness.}}
\label{fig:unsloth_Llama-3.2-1B-Instruct_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_unsloth_Llama-3.2-1B-Instruct.png}
\caption{\textbf{Llama-3.2-1B-Instruct: Cosine similarity matrix between persona vectors.}}
\label{fig:unsloth_Llama-3.2-1B-Instruct_ortho}
\end{figure}

\clearpage

\subsection{Gemma-2-2B-Instruct}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_gemma-2-2b-it_openness.png}
\caption{\textbf{Gemma-2-2B-Instruct: Layer-wise personality encoding profile for Openness.}}
\label{fig:unsloth_gemma-2-2b-it_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_unsloth_gemma-2-2b-it.png}
\caption{\textbf{Gemma-2-2B-Instruct: Cosine similarity matrix between persona vectors.}}
\label{fig:unsloth_gemma-2-2b-it_ortho}
\end{figure}

\clearpage

\subsection{Qwen2.5-1.5B-Instruct}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen2.5-1.5B-Instruct_openness.png}
\caption{\textbf{Qwen2.5-1.5B-Instruct: Layer-wise personality encoding profile for Openness.}}
\label{fig:Qwen_Qwen2.5-1.5B-Instruct_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_Qwen_Qwen2.5-1.5B-Instruct.png}
\caption{\textbf{Qwen2.5-1.5B-Instruct: Cosine similarity matrix between persona vectors.}}
\label{fig:Qwen_Qwen2.5-1.5B-Instruct_ortho}
\end{figure}

\clearpage

\subsection{Qwen2.5-7B-Instruct}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen2.5-7B-Instruct_openness.png}
\caption{\textbf{Qwen2.5-7B-Instruct: Layer-wise personality encoding profile for Openness.}}
\label{fig:Qwen_Qwen2.5-7B-Instruct_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_Qwen_Qwen2.5-7B-Instruct.png}
\caption{\textbf{Qwen2.5-7B-Instruct: Cosine similarity matrix between persona vectors.}}
\label{fig:Qwen_Qwen2.5-7B-Instruct_ortho}
\end{figure}

\clearpage

\subsection{Mistral-7B-Instruct-v0.1}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_mistralai_Mistral-7B-Instruct-v0.1_openness.png}
\caption{\textbf{Mistral-7B-Instruct-v0.1: Layer-wise personality encoding profile for Openness.}}
\label{fig:mistralai_Mistral-7B-Instruct-v0.1_layer}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/ortho_mistralai_Mistral-7B-Instruct-v0.1.png}
\caption{\textbf{Mistral-7B-Instruct-v0.1: Cosine similarity matrix between persona vectors.}}
\label{fig:mistralai_Mistral-7B-Instruct-v0.1_ortho}
\end{figure}

\clearpage

\subsection{Cross-Model Orthogonality Summary}

\begin{table}[h]
\caption{\textbf{Cross-model orthogonality summary.} Mean off-diagonal $|\cos|$ between persona vectors. Big Five independence varies across architectures.}
\label{tab:orthogonality_full}
\centering
\small
\begin{tabular}{lccc}
\toprule
& \textbf{Mean $|\cos|$ (Big Five)} & \textbf{Mean $|\cos|$ (all traits)} & \textbf{Common layer} \\
\midrule
Qwen3-0.6B & 0.107 & 0.171 & L14 \\
Qwen2.5-0.5B & 0.418 & 0.268 & L11 \\
TinyLlama-1.1B & 0.115 & 0.125 & L11 \\
Llama-3.2-1B & 0.054 & 0.054 & L6 \\
Qwen2.5-1.5B & 0.312 & 0.245 & L8 \\
Gemma-2-2B & 0.144 & 0.144 & L8 \\
Qwen2.5-7B & 0.285 & 0.198 & L12 \\
Mistral-7B & 0.089 & 0.112 & L10 \\
\bottomrule
\end{tabular}
\end{table}

% ============================================================
\section{Defense Mechanism Geometry}\label{app:defense}

Following Vaillant's hierarchy \citep{vaillant1986empirical}, we analyze nine defense mechanisms across five maturity levels. The full set of nine defense mechanisms on Qwen2.5-0.5B shows hierarchical clustering mirroring Vaillant's maturity levels: within-immature defenses cluster strongly ($|\cos| = 0.511$), neurotic defenses moderately ($0.189$), and mature defenses are near-orthogonal ($0.030$)---a 17-fold difference. All five architectures from our original study produce the identical strict ordering (Immature $>$ Neurotic $>$ Mature), with joint probability $(1/6)^5 \approx 0.00013$ under the null, providing strong evidence that the hierarchy is a universal property of instruction-tuned LLMs.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{figures/defense_mechanism_orthogonality_full.png}
\caption{\textbf{Defense mechanism cosine similarity matrix (Qwen2.5-0.5B).} The full 9$\times$9 matrix shows hierarchical clustering mirroring Vaillant's maturity levels: immature defenses (projection, denial, regression, reaction formation) cluster strongly ($|\cos|=0.511$), neurotic defenses (rationalization, intellectualization, displacement) moderately ($0.189$), and mature defenses (humor, sublimation) are near-orthogonal ($0.030$).}
\label{fig:defense_full}
\end{figure}

% ============================================================
\section{Multi-Framework Analysis: MBTI and Jungian Cognitive Functions}\label{app:multiframework}

This appendix presents the full per-model layer-wise encoding profiles for MBTI dimensions and Jungian cognitive functions, together with cross-framework comparative visualizations. These analyses were conducted on the original five models; replication on the three additional models is ongoing.

\subsection{Cross-Framework Comparison: Cohen's d by Trait and Model}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/cross_model_framework_comparison.png}
\caption{\textbf{Cross-model Cohen's d for all three frameworks.} Each panel shows per-trait effect sizes across the five tested models. Big Five traits (left) show the strongest and most consistent signal; MBTI dimensions (center) show weaker signal with the E/I dimension notably inverted in Qwen2.5; Jungian functions (right) show intermediate signal with high within-framework variance.}
\label{fig:cross_framework_comparison}
\end{figure}

\subsection{Framework Best-Layer Distribution}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{figures/framework_best_layer_distribution.png}
\caption{\textbf{Distribution of optimal layer position by framework.} Each point represents one (model, trait) pair. Jungian cognitive functions (green) peak significantly earlier in the network (median $\approx 0.35$) compared to Big Five (median $\approx 0.50$) or MBTI (median $\approx 0.50$), consistent with the hypothesis that cognitive function representations are encoded as more primitive processing features rather than high-level trait constructs.}
\label{fig:framework_layer_dist}
\end{figure}

\subsection{Jungian Cognitive Functions: Layer-wise Heatmap}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/jungian_layer_heatmap.png}
\caption{\textbf{Jungian cognitive function layer-wise Cohen's d heatmap (Qwen3-0.6B).} Rows are the eight cognitive functions; columns are normalized layer positions (0 = first layer, 1 = final layer). Color encodes Cohen's d (green = strong positive signal, red = negative). Stars (\textbf{$\star$}) mark each function's peak layer. Key patterns: (1) Ti and Fi (introverted judgment) peak in the earliest layers (L0--L4, normalized 0.00--0.14); (2) Te and Fe (extraverted judgment) peak in the middle layers (L13--L14, normalized 0.46--0.50); (3) Ne and Se (extraverted perceiving) peak early (normalized 0.07--0.14); (4) Ni (introverted intuition) uniquely peaks at the penultimate layer (normalized 0.96).}
\label{fig:jungian_heatmap}
\end{figure}

\subsection{MBTI Layer Profiles by Model}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen3-0.6B_mbti.png}
\caption{\textbf{Qwen3-0.6B: MBTI dimension layer profiles.}}
\label{fig:qwen3_mbti}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen2.5-0.5B-Instruct_mbti.png}
\caption{\textbf{Qwen2.5-0.5B: MBTI dimension layer profiles.} The E/I subplot shows negative or near-zero d across all layers---the inversion anomaly.}
\label{fig:qwen25_mbti}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_TinyLlama_TinyLlama-1.1B-Chat-v1.0_mbti.png}
\caption{\textbf{TinyLlama-1.1B: MBTI dimension layer profiles.}}
\label{fig:tinyllama_mbti}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_Llama-3.2-1B-Instruct_mbti.png}
\caption{\textbf{Llama-3.2-1B: MBTI dimension layer profiles.}}
\label{fig:llama32_mbti}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_gemma-2-2b-it_mbti.png}
\caption{\textbf{Gemma-2-2B: MBTI dimension layer profiles.}}
\label{fig:gemma2_mbti}
\end{figure}

\clearpage

\subsection{Jungian Cognitive Function Layer Profiles by Model}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen3-0.6B_jungian.png}
\caption{\textbf{Qwen3-0.6B: Jungian cognitive function layer profiles.} Ti and Fi peak sharply at the earliest layers (L0--L4); Te and Fe peak in middle layers; Ni uniquely peaks at the penultimate layer.}
\label{fig:qwen3_jungian}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_Qwen_Qwen2.5-0.5B-Instruct_jungian.png}
\caption{\textbf{Qwen2.5-0.5B: Jungian cognitive function layer profiles.} Ti achieves an exceptionally high d=6.88 at an early layer.}
\label{fig:qwen25_jungian}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_TinyLlama_TinyLlama-1.1B-Chat-v1.0_jungian.png}
\caption{\textbf{TinyLlama-1.1B: Jungian cognitive function layer profiles.}}
\label{fig:tinyllama_jungian}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_Llama-3.2-1B-Instruct_jungian.png}
\caption{\textbf{Llama-3.2-1B: Jungian cognitive function layer profiles.}}
\label{fig:llama32_jungian}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/layer_profile_unsloth_gemma-2-2b-it_jungian.png}
\caption{\textbf{Gemma-2-2B: Jungian cognitive function layer profiles.}}
\label{fig:gemma2_jungian}
\end{figure}

\clearpage

% ============================================================
\section{Control Experiments}\label{app:control}

\subsection{Position-Swap Control}\label{app:position_swap}

To address concerns that user-token causal dominance might reflect positional artifacts (recency effect), we reverse the prompt layout: placing the neutral scenario in the system role and the persona instruction in the user role.

Results reveal that causal influence remains concentrated at sequence-final positions regardless of content placement. This confirms a positional component consistent with the autoregressive bottleneck: the model's output distribution is most sensitive to activations nearest the generation point. However, early-layer specificity persists regardless of content placement, confirming attention-mediated propagation as the operative mechanism.

\subsection{Paraphrase Control}\label{app:paraphrase}

To rule out keyword confounding, we train probes on Template A (original prompts) and test on Template B (paraphrased versions), achieving 91.2\% mean cross-template accuracy (range: 87.5--100\%). Openness achieves perfect bidirectional transfer (1.000), confirming abstract conceptual representation rather than surface lexical cues.

\subsection{Out-of-Domain Generalization}\label{app:ood}

When extracting vectors from non-overlapping 50\% splits, OOD stability is strong for larger models (Llama-3.2 mean 0.988, Gemma-2 mean 0.914) and moderate for smaller models (Qwen2.5 mean 0.801). Openness shows exceptional stability (mean $\cos=0.935$), suggesting abstract conceptual encoding, while Extraversion and Agreeableness show context-dependent encoding.

% ============================================================
\section{Algorithm Pseudocode}\label{app:algorithm}

\begin{figure}[H]
\centering
\fbox{\parbox{0.92\textwidth}{\small
\textbf{Algorithm 1:} PersonaLens --- Standardized Personality Vector Extraction\\[4pt]
\textbf{Input:} Model $f_\theta$, Trait $t$, Contrastive pairs $\{(x^+_i, x^-_i)\}_{i=1}^K$\\
\textbf{Output:} Persona vectors $\{v_\ell\}_{\ell=0}^{L-1}$, Encoding profile, Causal map\\[4pt]
\textit{// Phase 2: Collect Activations}\\
1:\quad \textbf{for} $i = 1$ \textbf{to} $K$ \textbf{do}\\
2:\quad\quad $\{h_\ell(x^+_i)\}_{\ell=0}^{L-1} \leftarrow \text{ForwardPass}(f_\theta, x^+_i)$\\
3:\quad\quad $\{h_\ell(x^-_i)\}_{\ell=0}^{L-1} \leftarrow \text{ForwardPass}(f_\theta, x^-_i)$\\
4:\quad \textbf{end for}\\[4pt]
\textit{// Phase 3: Extract Vectors}\\
5:\quad \textbf{for} $\ell = 0$ \textbf{to} $L-1$ \textbf{do}\\
6:\quad\quad $v^{\text{diff}}_\ell \leftarrow \frac{1}{K}\sum_i [h_\ell(x^+_i) - h_\ell(x^-_i)]$ \hfill \textit{// Mean difference}\\
7:\quad\quad $w_\ell, a_\ell \leftarrow \text{LogisticRegression}(H^+_\ell, H^-_\ell)$ \hfill \textit{// Linear probe}\\
8:\quad\quad $u_\ell, \rho_\ell \leftarrow \text{PCA}(\{h_\ell(x^+_i) - h_\ell(x^-_i)\}_i)$ \hfill \textit{// PCA direction}\\
9:\quad \textbf{end for}\\[4pt]
\textit{// Phase 4: Causal Localization}\\
10:\quad \textbf{for} $\ell = 0$ \textbf{to} $L-1$ \textbf{do}\\
11:\quad\quad $\mathcal{I}(\ell) \leftarrow D_{\text{KL}}(\text{Clean}(x^+) \| \text{Patched}_\ell(x^+, h_\ell(x^-)))$\\
12:\quad \textbf{end for}\\
13:\quad \textbf{return} $\{v_\ell, a_\ell, \|v_\ell\|, \rho_\ell, \mathcal{I}(\ell)\}_\ell$
}}
\label{alg:personalens}
\end{figure}

% ============================================================
\section{Perplexity Calculation Methodology}\label{app:ppl}

\begin{equation}
\text{PPL} = \exp\left(-\frac{1}{T}\sum_{t=1}^{T} \log p_\theta(w_t | w_{<t})\right)
\end{equation}
where $p_\theta$ is the unmodified (un-steered) model's probability distribution. We compute perplexity on the \emph{full sequence} (prompt + generated response), using the base model without steering hooks active. The threshold of PPL $< 10$ was selected empirically: manual inspection revealed that outputs with PPL $> 10$ consistently exhibited token-level repetition loops, grammatical breakdown, or semantic incoherence. We acknowledge that absolute PPL values are not directly comparable across architectures due to different tokenizers and vocabulary sizes; the threshold serves as a within-model fluency gate rather than a cross-model metric.

\end{document}
